my_fruits = {"apple", "banana", "cherry"}
print(my_fruits)

print(type(my_fruits)) #it will show : set

posts_ids = {151, 2,3,4,5,4,5}
print(posts_ids) # it will show just unique  elements , so set only shows the unique elements

my_set = {10,10,5,5}
print(my_set) # it will show only unique  elements, so set only shows the unique elements
print(len(my_set)) # it will show only unique elements